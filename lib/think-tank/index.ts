/**
 * NomosX Think Tank Library - Main Export
 */

// Core types (agents, pipeline)
export * from "./types";

// Verticals configuration
export * from "./verticals";

// Publication templates
export * from "./templates";

// UI types are exported separately to avoid conflicts
// Import directly from "@/lib/think-tank/ui-types" for UI components
